#include <iostream>
#include "head_16.hpp"

template <typename T>
T Max(T t1, T t2)
{
    x = 5;
    // return (t1 > t2) ? t1 : t2;   
}

int main()
{
    int arr[12];

    // std::cout << Max<int>(3, 5) << " " << Max<double>(3.7, 12.9) << " ";
    // std::cout << Max<int*>(arr, arr + 8) << std::endl;
    // std::cout << Max('a', 'b') << std::endl;
    // std::cout << Max(2.3f, 5.5f) << std::endl;

    return 0;
}